import { defineType, defineField } from "sanity";

export default defineType({
  name: "portrait",
  title: "Portrait Photography",
  type: "document",
  fields: [
    defineField({
      name: "title",
      title: "Title",
      type: "string",
    }),
    defineField({
      name: "image",
      title: "Image",
      type: "image",
      options: {
        hotspot: true,
      },
    }),
  ],
});
